django-cors-headers==4.9.0
fastapi==0.109.0
uvicorn[standard]==0.27.0
pandas==2.1.4
numpy==1.26.3
pydantic==2.5.3
python-multipart==0.0.6



# ── Web Framework ─────────────────────────────────────────────────────────────
Django==6.0.1
daphne==4.2.1           # ASGI server (required for Django async / WebSocket support)
asgiref==3.11.0         # Django's async bridge (sync_to_async used in FastAPI layer)

# ── Template & Form Utilities ─────────────────────────────────────────────────
django-crispy-forms==2.5
crispy-bootstrap5==2025.6

# ── FastAPI (REST API layer) ───────────────────────────────────────────────────
fastapi==0.136.3
starlette==1.2.1        # FastAPI foundation (CORS middleware imported directly)
pydantic==2.12.5        # Request / response schema validation
uvicorn==0.49.0         # ASGI server for running the app in production

# ── DHS Dataset Processing ────────────────────────────────────────────────────
pyreadstat==1.3.5       # Read Stata .DTA files
pandas==2.3.3           # DataFrame operations for indicator computation
numpy==1.26.4           # Numerical helpers used in indicator functions

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.2.1    # Load environment variables from a .env file
requests==2.31.0



      # For making HTTP requests to external APIs (if needed)